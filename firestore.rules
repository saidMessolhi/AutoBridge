rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for trial demo to make multi-device testing work 
    // seamlessly without requiring actual user authentication setup.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
